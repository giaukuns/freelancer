name: Build and Deploy
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    concurrency: ci-${{ github.ref }}
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - name: Deploy 🚀
        uses: giaukuns/freelancer
        with:
          branch: gh-pages
          folder: build
          repositorty-name: giaukuns/freelancer
          target-folder: public